import React from "react";
import Checkout from "../../containers/Checkout";

// eslint-disable-next-line
const Cart = ({}) => {
  return (
    <div>
      <Checkout />
    </div>
  );
};

export default Cart;
